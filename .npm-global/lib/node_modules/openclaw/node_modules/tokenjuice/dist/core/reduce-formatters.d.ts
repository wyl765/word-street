import { type CompactionMetadata } from "./compaction-metadata.js";
import type { ToolExecutionInput } from "../types.js";
export declare function rewriteGitStatusLines(lines: string[]): string[];
export declare function rewriteSearchLines(lines: string[]): {
    lines: string[];
    compaction?: CompactionMetadata;
};
export declare function rewriteGitDiffLines(lines: string[]): {
    lines: string[];
    compaction?: CompactionMetadata;
};
export declare function rewriteGhLines(lines: string[], input: ToolExecutionInput): {
    lines: string[];
    compaction?: CompactionMetadata;
};
