import type { Pi } from "./pi-types.js";
export type PiExtensionRuntimeConfig = {
    extensionCommand: string;
};
export declare function createTokenjuicePiExtension(config: PiExtensionRuntimeConfig): (pi: Pi) => void;
